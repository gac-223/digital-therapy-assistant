package com.digitaltherapyassistant.cli;

import static org.mockito.ArgumentMatchers.eq;
import static org.mockito.Mockito.mock;
import static org.mockito.Mockito.verify;

import java.util.Scanner;

import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.extension.ExtendWith;
import org.mockito.InjectMocks;
import org.mockito.Mock;
import org.mockito.junit.jupiter.MockitoExtension;

@ExtendWith(MockitoExtension.class)
class DigitalTherapyCLIClientTest {

    // @Mock private MenuHandler menuHandler;
    // @Mock private Scanner in;

    // @InjectMocks DigitalTherapyCLIClient digitalTherapyCLIClient;

    // @Test
    // void runDelegatesToMenuHandlerAndClosesScanner() throws Exception {
    //     digitalTherapyCLIClient.run();
    //     verify(menuHandler).runMenu(eq("Main Menu"), eq(in));
    //     verify(in).close();
    // }
}
